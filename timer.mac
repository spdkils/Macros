#turbo
Sub Main
/declare seconds int outer 0
/echo -------- TIMER ----------
:TOP
/if (!${Me.CombatState.Equal[Combat]}) /GOTO :SKIP
/delay 1s
/varcalc seconds ${seconds}+1
/echo ${seconds}
:SKIP
/GOTO :TOP
/return
| endsub