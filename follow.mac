|- Follow

Sub Main
	/if (!${Group}) /return
	/declare loop int outer
	/for loop 1 to ${Group}
			/target ${Group.Member[${loop}].Name}
			/delay 2
			|/echo ${Math.Calc[(${loop}*75)+50]}
			/say ^botfollowdistance set ${Math.Calc[(${loop}*75)+50]}
	/next loop

/return
| endsub
