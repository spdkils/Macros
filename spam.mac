#turbo

Sub Main
	:TOP
	/call Mana
	/if (${Me.SpellReady[1]} && ${Me.PctMana}>10) /cast 1
	/delay 10s !${Me.Casting.ID}
	/GOTO :TOP
/return
| endsub

Sub Mana
/if (${Me.PctMana}<10 && !${Me.Sitting}) {
	/sit
	/delay 60s
}
/return
| endsub
